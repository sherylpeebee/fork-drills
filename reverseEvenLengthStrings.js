//i know this is scrap, but for the sake of pull request practice.
//discard me.

var reverseEvenLengthStrings = function(someArray){

  var i;
  for (i = 0; i < someArray.length; i ++){
  if i.length%2{
    return
  }
  elseif{
    return i.split();
  }
   var i.split = evenString;

    reversedString = evenString.split.reverse();

    joinedString = reversedString.join()


}
reverseEvenLengthStrings(['cat','elum','horse','jquery'])
